package com.example.demo.interfaces;

public interface ValidationGroups {
	//Apenas para marcação, indicando para o @Valid que
	//ele deve ou não validar o campo em questao
	public interface ClientId{};
}
